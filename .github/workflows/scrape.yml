name: Fetch and Update Stream JSON

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  fetch-json:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          ref: main

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install Dependencies
        run: |
          cd scraper
          npm install

      - name: Run Fetch Script
        env:
          HOTSTAR_M3U: ${{ secrets.HOTSTAR_M3U }}
          ZEE5_M3U: ${{ secrets.ZEE5_M3U }}
          SPORTS1_JSON: ${{ secrets.SPORTS1_JSON }}
          SPORTS2_JSON: ${{ secrets.SPORTS2_JSON }}
          SONYLIV_M3U: ${{ secrets.SONYLIV_M3U }}
          SUNXT_JSON: ${{ secrets.SUNXT_JSON }}
          NEW_M3U: ${{ secrets.NEW_M3U }}
        run: |
          cd scraper
          npm start

      - name: Commit and Push stream.m3u
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git remote set-url origin https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git

          git pull --rebase origin main

          cp scraper/stream.m3u stream.m3u
          git add stream.m3u

          git diff-index --quiet HEAD || git commit -m "📥 updated stream.m3u from remote source"

          if [ "$(git rev-parse HEAD)" != "$(git rev-parse @{u})" ]; then
            git push origin main
          else
            echo "No changes to push."
          fi
